# ملاحظات أمنية مفتوحة — v34

آخر تحديث لـ `npm audit`: **0 حرجة (critical)**، البقية موثقة أدناه مع سبب عدم الإصلاح.

## غير قابلة للإصلاح حالياً (بدون كسر شيء)

### nodemailer — SSRF عبر خيار `raw` (GHSA-p6gq-j5cr-w38f)
- **النطاق المتأثر:** كل نسخة `<= 9.0.0`. الإصلاح الوحيد هو `9.0.1+`.
- **لماذا لم نرفعها:** `next-auth@5.0.0-beta.32` (أحدث نسخة متاحة) يحصر `nodemailer` بـ `^7.0.7 || ^8.0.5` كـ peer dependency. رفعها لـ 9.x يكسر التثبيت من جديد (نفس مشكلة v26 الأصلية).
- **لماذا الخطر منخفض عملياً:** الثغرة تتطلب استخدام خيار `raw` أو `jsonTransport` عند إرسال الرسالة. كودنا (`src/lib/email/sender.ts`) لا يستخدم أياً منهما — فقط `to/subject/html` عبر `sendMail` القياسية.
- **الإجراء المطلوب لاحقاً:** راقب صدور نسخة `next-auth` تدعم `nodemailer@^9`، وارفعها حينها.

### xlsx (SheetJS) — Prototype Pollution + ReDoS
- **النطاق المتأثر:** كل نسخة منشورة على npm (`0.18.5` وما قبلها).
- **لماذا لم نرفعها:** SheetJS توقفوا عن نشر إصدارات مصححة على سجل npm العام منذ 2023 — الإصدارات المصححة (`0.20.2+`) متوفرة فقط عبر CDN الخاص بهم (`cdn.sheetjs.com`)، مو عبر `npm install`. هذا قرار من المشروع نفسه مو تقصير من جهتنا.
- **الإجراء المطلوب لاحقاً:** إذا الأولوية عالية، بدّل مصدر التثبيت لـ `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` بدل اسم الحزمة العادي بـ `package.json` (يتطلب اختبار كامل لوظائف تصدير Excel).

### next / postcss — DoS مُغلَّفة داخل Next.js نفسه
- **النطاق المتأثر:** `postcss <=8.5.22` مُجمَّعة (bundled) داخل `next@15.5.22` نفسه — مو تبعية مباشرة نقدر نرفعها لحالها.
- **لماذا لم نرفعها:** الإصلاح الوحيد المعروف حالياً هو `next@16.3.0` — قفزة نسخة رئيسية (major) غير مختبرة بهذا المشروع، خطر كسر حقيقي بدون دورة اختبار كاملة.
- **ملاحظة:** كان هذا الفحص يشمل أيضاً `sharp` (كانت مُجمَّعة بنفس next) — تم حلها بإضافة `overrides.sharp: "^0.35.3"` بـ `package.json` (v27)، فانخفضت شدة تحذير `next` من high إلى moderate.

## أدوات تطوير فقط (لا تدخل حزمة الإنتاج)
`drizzle-kit` (رُفعت لـ `0.31.10`، بقي تحذير moderate من نسخة فرعية لـ `esbuild`)، `@esbuild-kit/*`، `esbuild` — هذي كلها تُستخدم فقط بأوامر `db:generate`/`db:studio` أثناء التطوير، ما تنشحن ضمن `npm run build` أو الإنتاج الفعلي. أولوية منخفضة.

## v31 — إصلاحات صحة تشغيلية (Operational Integrity) — مبنية على مراجعة خارجية

الأصل: مراجعة استلمناها تشكك بصحة (correctness) النظام تحت تزامن وحمل حقيقيين — بصرف النظر عن وجود جداول/constraints/migrations. راجعنا كل نقطة ضد الكود الفعلي، وثبتت عدة نقاط كثغرات حقيقية. هذا سجل الإصلاحات وحدودها المتبقية، بصراحة:

### 1) Race Condition بخصم المخزون — **تم الإصلاح**
كان الفحص (`currentStock < quantity`) والخصم عمليتين منفصلتين (قراءة ثم UPDATE)، فطلبان متزامنان على نفس الصنف يقدران يمرّا الفحص معاً ويوصلان الرصيد لسالب فعلي. الإصلاح: `src/lib/inventory/stock-movement.ts` — الفحص والخصم بعبارة `UPDATE ... WHERE current_stock >= qty` واحدة ذرّية. مضاف أيضاً قيد `CHECK (current_stock >= 0)` على مستوى قاعدة البيانات كخط دفاع أخير (migration 013).
**الحد المتبقي:** الاختبار الحالي (`stock-movement.test.ts`) يثبت صحة *خوارزمية* الشرط تحت تزامن محاكى، وليس تزامناً حقيقياً ضد Postgres فعلي بعدة اتصالات — هذا يحتاج اختبار تكامل بقاعدة حقيقية (CI منفصل)، لم يُنفَّذ هنا.

### 2) ترحيل قيد بعد إغلاق الفترة المحاسبية — **تم الإصلاح**
`postJournalEntry` كان يتحقق من إغلاق الفترة عند *الإنشاء* فقط. قيد أُنشئ والفترة مفتوحة، ثم أُقفلت الفترة، كان يمكن ترحيله لاحقاً بلا مانع. الإصلاح: نفس الفحص يتكرر عند الترحيل نفسه، داخل نفس الـ transaction.

### 3) غياب Idempotency — **تم الإصلاح جزئياً**
مضافة طبقة `src/lib/idempotency/guard.ts` (reserve-before-execute عبر unique index بقاعدة البيانات، آمنة تحت تزامن حقيقي) ومربوطة بـ `createJournalEntry` و`createDistribution`.
**الحد المتبقي:** لم تُربط بعد بباقي العمليات المالية/المخزنية الحساسة (مثلاً receipts، payroll runs، purchase orders) — هذا نمط جاهز الآن، يحتاج فقط تطبيقه على بقية الـ actions حسب الأولوية.

### 4) غياب محرك Reversal للقيود — **تم الإصلاح**
لم تكن توجد أي طريقة لتصحيح قيد مرحَّل سوى تعديله مباشرة (يدمّر الأثر المحاسبي). أُضيفت `reverseJournalEntry` — تنشئ قيداً عكسياً مرتبطاً بالأصلي، ترفض العكس داخل فترة مغلقة، وتطلب سبباً إلزامياً.

### 5) لا يوجد دفتر مخزون حقيقي (تكلفة) — **تم الإصلاح جزئياً**
أُضيف جدول `stock_ledger` (migration 013) بمنهج **AVCO (متوسط مرجّح)** — اخترناه بدل FIFO لأنه لا يحتاج جدول دفعات (lots) منفصل ليعمل بشكل صحيح تحت التزامن بعملية SQL ذرية واحدة.
**الحد المتبقي:** `items.currentStock` ما زال يُستخدم كـ cache للعرض السريع، ومصدر الحقيقة الفعلي الآن `stock_ledger` — لكن التسوية الدورية (reconciliation) بين الاثنين لم تُبرمج بعد كوظيفة مجدولة. لو تحتاجون تتبّع دفعات حقيقي (FIFO مع تواريخ صلاحية/lots) فهذا تصميم منفصل أكبر لم يُنفَّذ.

### 6) غياب اختبارات concurrency/integration — **تم الإصلاح جزئياً**
أُضيفت اختبارات لكل الإصلاحات أعلاه (`npx vitest run` → 250/250 ناجحة، `npx tsc --noEmit` نظيف). **لكن**: كل هذه اختبارات وحدة (unit) بقاعدة بيانات مُحاكاة (mocked)، وليست اختبارات تكامل ضد Postgres حقيقي بعدة اتصالات فعلية أو تحت حمل (load/stress). هذا الفرق مهم ولم يُغلَق — يحتاج بيئة CI بقاعدة Postgres فعلية.

### 7) سجل التدقيق غير tamper-evident — **تم الإصلاح جزئياً، بحدود موثقة بصراحة**
أُضيفت سلسلة تجزئة (`prevHash`/`hash`) لكل سطر بـ `audit_logs`، مع دالة `verifyAuditChain` للتحقق الدوري.
**الحد الجوهري المتبقي (لم يُحَل، ولا يُحَل بكود التطبيق وحده):** هذا *tamper-evident* (يكتشف التلاعب) وليس *immutable* بشكل مطلق. من يملك صلاحية DB owner/superuser يقدر يعدّل أي عمود — بما فيها `hash`/`prevHash` أنفسهما — ويعيد حساب السلسلة كاملة، لأن الحساب يتم من نفس تطبيق Node.js الذي له صلاحية الكتابة الكاملة على الجدول، لا دور منفصل بصلاحية INSERT-only. الحل الكامل يحتاج قراراً بنيوياً خارج نطاق هذا الكود: دور DB منفصل بـ `REVOKE UPDATE, DELETE`، و/أو HMAC بمفتاح خارج القاعدة (KMS)، و/أو إرسال دوري لآخر hash لنظام خارجي مستقل (WORM storage) كمرجع لا يقدر مالك القاعدة تعديله.

### 8) لا يوجد اختبار IDOR فعلي — **تم الإصلاح جزئياً + اكتُشفت ثغرة إضافية أثناء الإصلاح**
أثناء كتابة اختبار المحاكاة لـ `createDistribution`، تبيّن أن `assertOrgMatches` كانت تتحقق فقط من أن `organizationId` المُرسَل من العميل يطابق جلسة المستخدم — لكن **لا تتحقق أن `beneficiaryId`/`grantId` المرسَلين فعلاً تابعان لنفس المنظمة**. مستخدم من منظمة A كان يقدر يمرّر `organizationId` الصحيح لمنظمته (يمر الفحص) مع `beneficiaryId`/`grantId` تابعين لمنظمة B، فيربط بيانات عبر حدود المستأجرين (tenants) دون منع. تم إصلاحها بالتحقق الصريح من ملكية السجلين قبل أي كتابة (`src/modules/beneficiaries/actions.ts`).
**الحد المتبقي:** هذا النمط (فحص org على السجلات المرتبطة، لا فقط على `organizationId` المُرسَل صراحة) لم يُراجَع بعد بشكل منهجي عبر باقي الـ actions في كل الموديولات — فقط في `createDistribution`. مراجعة IDOR شاملة أوسع (موثّقة سابقاً بـ `final-checklist.md`) ركّزت على انتحال `organizationId` نفسه، لا على تسريب عبر foreign keys مرتبطة. **يُنصح بمراجعة نفس النمط على الأقل في:** `procurement` (vendorId/POid عبر منظمات)، `hr` (employeeId)، `fleet` (vehicleId)، وأي action آخر يستقبل معرّفات لسجلات مرتبطة من العميل مباشرة.

**خلاصة صريحة:** 5 من 8 نقاط أُصلحت بالكامل على مستوى منطق التطبيق (1، 2، 4، وجزئياً غيرها)، والبقية (3، 5، 6، 7، 8) أُصلحت جزئياً مع توثيق دقيق للفجوة المتبقية بدل ادّعاء إغلاق كامل. لا يوجد أي بند هنا اعتُبر "محلولاً" بدون دليل تشغيلي فعلي (`tsc`/`vitest` ناجحَين).

## v32 — مراجعة خارجية ثانية أعمق + تصحيح ذاتي

مراجعة خارجية ثانية (أدق من الأولى، بأسطر كود محددة) فحصت الكود **قبل** إصلاحات v31 وكشفت طبقة أعمق من الفجوات. راجعنا كل نقطة، صححنا خطأ ارتكبناه نحن أنفسنا بv31، وأصلحنا كل ما تبيّن أنه حقيقي وغير مغطى بعد.

### تصحيح ذاتي مهم: دمج جدولي مخزون متوازيين
بv31 أنشأنا جدول `stock_ledger` جديد للرصيد المتحرك/التكلفة المرجّحة **دون الانتباه** أن جدول `stock_movements` كان موجوداً أصلاً بالسكيما (بحقول أفضل فعلياً — `journalEntryId`, `grantId` مباشرة) وغير مُستخدَم بأي كود. بدل جدولين متوازيين لنفس الغرض، دُمج كل شي بجدول `stock_movements` الأصلي (أعمدة `balanceQty`/`balanceAvgCost`/`idempotencyKey` أُضيفت له). migration 013 أُعيد كتابته بالكامل ليعكس هذا — بما أنه لم يُشغَّل بعد ضد أي DB حقيقي (موثّق سابقاً)، لا حاجة لـ down-migration.

### CHECK constraints + Triggers على مستوى القاعدة — **جديد بالكامل**
كانت **صفر** CHECK constraints بكل قاعدة البيانات (فقط NOT NULL/FK/UNIQUE) — توازن القيد، القيم المسموحة، الكميات الموجبة، كلها TypeScript/Zod فقط. الآن (migration 013):
- `CHECK` على `journal_lines` (مبالغ غير سالبة، ومنع سطر فيه مدين ودائن معاً)
- `CHECK` على `stock_movements` (كمية موجبة، movement_type ضمن قيم صحيحة)
- `CHECK` على `journal_entries.entry_type`
- **Trigger** يمنع تعديل/حذف `journal_lines` بعد ترحيل القيد الأب — على مستوى القاعدة نفسها، مو منطق التطبيق فقط
- **Trigger** يمنع تعديل مبالغ/فترة/حالة ترحيل `journal_entries` بعد الترحيل (يسمح فقط بحقول العكس isReversed/reversalEntryId)
- **Trigger** يفرض توازن مدين=دائن عند الترحيل كخط دفاع أخير DB-level
- **Trigger** يمنع أي `UPDATE`/`DELETE` على `audit_logs` نهائياً — بدون حاجة لدور DB منفصل (REVOKE)، الرفض من داخل الدالة نفسها. **الحد المتبقي**: من يملك صلاحية superuser يقدر يعطّل التريجر نفسه؛ هذا يحتاج منع صلاحية superuser عن الاتصال التطبيقي على مستوى البنية التحتية، خارج نطاق الكود.

### Audit log صار ذرّياً فعلاً مع العملية — **تم الإصلاح**
كان `createAuditLog` يُستدعى بعد commit العملية الأساسية، مغلّف بـ try/catch يبتلع الخطأ. الآن تقبل `tx` اختيارية — لو انمرّرت (من `createJournalEntry`, `postJournalEntry`, `reverseJournalEntry`, `createDistribution`)، الإدراج يصير **داخل نفس المعاملة** بدون ابتلاع الخطأ: فشل تسجيل التدقيق يُسقط العملية كلها. الاستدعاء بدون `tx` (بقية الموديولات، لم تُحدَّث بعد) يحتفظ بالسلوك القديم مؤقتاً.

### Pagination — الاكتشاف الأهم بهذي الجولة
الفحص الأولي (22 استعلام `findMany` بـ`actions.ts`/`lib/`) بالتفصيل طلع **أغلبها مقيّد أصلاً بمفتاح عمل طبيعي** (منحة واحدة، أمر شراء واحد، مرحلة موافقة واحدة، دورة رواتب واحدة) — حجمها محدود بغض النظر عن نمو الجدول، مو نفس خطورة استعلام قائمة بلا قيد. **الخطر الحقيقي الأكبر كان بمكان لم يُفحص سابقاً**: **59 استدعاء `findMany` داخل ملفات `page.tsx`** (صفحات القوائم الفعلية). تحققنا وأصلحنا 4 صفحات مؤكَّدة بلا أي حد: `vendors`, `grants`, `procurement`, `logistics/fleet` (أداة موحّدة `src/lib/pagination/parse.ts` + `src/components/shared/PaginationBar.tsx`).
**ملاحظة مهمة اكتُشفت أثناء إصلاح `fleet`**: تنبيهات الصيانة/الرخص المنتهية كانت تُحسب من نفس مصفوفة العرض — لو قُسِّمت لصفحات بسذاجة، التنبيهات كانت رح تفوت سجلات بصفحات أخرى (خطأ صامت). أُصلح بفصل استعلام التنبيهات (خفيف، أعمدة قليلة، غير مُقسَّم) عن استعلام العرض (كامل، مُقسَّم).
**الحد المتبقي**: 55 استدعاء آخر بملفات `page.tsx` لم يُراجَع بعد لهذا الجولة — القائمة الكاملة تحتاج مراجعة صفحة صفحة (بعضها احتياجه للـ"عرض شامل" زي kanban، بعضها listing حقيقي).

### GRN (استلام بضاعة) — **بُني من الصفر**
السبب الجذري اللي فسّر ليش "ما فيه كود يستلم بضاعة فعلياً": **لا يوجد أي ربط بين بند أمر الشراء (`purchase_order_items`) وكتالوج أصناف المخزون (`items`)** — `itemDescription` نص حر فقط. أُضيف عمود `item_id` اختياري (migration 014؛ اختياري لأن كثير من بنود الشراء خدمات/مشتريات لمرة واحدة لا تحتاج تتبّع مخزون). بُنيت `createGoodsReceiptNote` كاملة:
- تُنشئ GRN + سطوره فعلياً، تتحقق من عدم تجاوز الكمية المطلوبة تراكمياً
- تستدعي `receiveStock` بالكمية **المقبولة فقط** (مستلمة ناقص مرفوضة) للبنود المرتبطة بصنف مخزون، مع `grantId` من أمر الشراء تلقائياً للربط المحاسبي
- كل شي بمعاملة واحدة (GRN + PO update + مخزون + تدقيق) — فشل أي جزء يُلغي الكل
- idempotency مدمجة (نفس نمط بقية العمليات)

**الحد المتبقي**: لا يوجد UI (نموذج/صفحة) لاستدعاء هذه الدالة بعد — الـ action موجودة ومُختبَرة، الواجهة لم تُبنَ. كمان لا يوجد بعد ربط تلقائي بين `receiveStock`/`issueStock` وإنشاء قيد محاسبي تلقائي (كان موجوداً فقط بمنطق الإهلاك depreciation) — لو تحتاجون قيد GL تلقائي عند كل استلام/صرف مخزون، هذا تصميم إضافي منفصل.

**تحقق فعلي**: `npx tsc --noEmit` نظيف، `npx vitest run` → **261/261 ناجح** (كانت 250 بنهاية v31؛ 11 اختباراً جديداً لـpagination وGRN).

## v33 — مراجعة ذاتية طلبها المستخدم صراحة: "تفحّص من جديد، لا أريد تناقضات أو كسر بسبب عدم اعتماد إجراءات معقدة كأودو"

هذي جولة مراجعة ذاتية (مو مبنية على مراجعة خارجية) — راجعنا كل تعديلات v31/v32 ضد بعضها بحثاً عن تناقضات، ونقاط كسر ناتجة عن تبسيط زايد مقارنة بأنظمة ناضجة زي Odoo. لقينا مشكلتين حقيقيتين وأصلحناهما:

### 1) CHECK constraints جديدة بلا تحقق تطبيقي مطابق — **كانت ستكسر تجربة المستخدم**
أضفنا بv32 عدة CHECK constraints بقاعدة البيانات (منع سطر فيه مدين ودائن معاً، منع مبالغ سالبة) **بدون** أي تحقق مطابق بطبقة التطبيق. لو صار هذا الخطأ فعلياً، المستخدم كان سيشوف رسالة Postgres خام (`new row violates check constraint "journal_lines_not_both_debit_and_credit"`) بدل رسالة عربية مفهومة — بالضبط المشكلة التي يحذّر منها السؤال: الاعتماد على DB وحده بدون الطبقة التطبيقية فوقه، خلافاً لنمط التحقق متعدد الطبقات (ORM ثم DB) المعتمد بأنظمة ناضجة كـOdoo. أُضيف تحقق تطبيقي مطابق تماماً بـ`createJournalEntryInner` (مدين+دائن معاً، مبلغ سالب، سطر فارغ، `entryType` خارج القيم المسموحة) — DB الآن خط دفاع أخير حقيقي، مو خط الدفاع الوحيد.

### 2) حجز Idempotency عالق للأبد عند فشل العملية — **كانت الأخطر فعلياً**
اكتشفنا أن `withIdempotency` كانت تحجز المفتاح، **لكن لا تُفكّه أبداً لو فشلت العملية**:
- لو `fn()` أرجعت `{success:false}` (فشل تحقق) — كانت تُخزَّن كنتيجة نهائية، فأي إعادة محاولة لاحقة (حتى بعد تصحيح البيانات) تاخذ نفس رسالة الفشل القديمة للأبد.
- **الأخطر**: لو `fn()` رمت استثناءً (`throw`) — وهذا هو المسار **الأشيع فعلياً** بكودنا (`createDistribution` و`createGoodsReceiptNote` يرميان استثناءً من داخل `db.transaction` عند فشل تحقق عمل مثل نقص المخزون أو تجاوز كمية الاستلام) — الحجز كان يبقى **عالقاً للأبد** بـ`responseJson: null`، وأي إعادة محاولة تصطدم بـ`IdempotencyInProgressError` بشكل دائم، حتى لو المستخدم صحّح كل شي.

أُصلح كامل: `withIdempotency` تلتقط أي رمي أو نتيجة فشل صريحة، تفكّ الحجز فوراً (حذف السطر)، وتعيد رمي/إرجاع نفس الخطأ للمستدعي — سلوك الخطأ نفسه من الخارج، بدون حجز عالق من الداخل.

### فحوصات أخرى تمت (نتيجتها: لا مشكلة، موثّقة للشفافية)
- لا يوجد أي مسار كود آخر يعدّل `items.currentStock` مباشرة متجاوزاً الدوال الذرّية الجديدة (`stockLedger` القديم — صفر مراجع متبقية بعد الدمج)
- لا يوجد أي `UPDATE`/`DELETE` آخر على `journal_lines`/`journal_entries` خارج الملفات التي عدّلناها — الـtriggers الجديدة لن تصطدم بكود موجود مسبقاً
- كل قيم `entryType` المُدرَجة فعلياً بالكود (`depreciation`, `reversal`, وقيم `createJournalEntry` العادية) متوافقة مع CHECK الجديد
- ترتيب الاعتماديات بـmigration 001 سليم (drizzle-kit ينشئ الجداول أولاً بدون FK inline، ثم يضيف كل الـFK بـALTER منفصل بآخر الملف) — لا مشكلة ترتيب رغم أن `stock_movements` يُنشأ قبل `warehouses` بالملف
- الـseed files لا تلمس `journal_lines`/`stock_movements`/`items` إطلاقاً — لا خطر تعارض بيانات تجريبية مع القيود الجديدة
- migration 013 غير idempotent حرفياً (`ADD CONSTRAINT` بدون `IF NOT EXISTS` — Postgres لا يدعمها لـCHECK) لكن `run-migrations.ts` يتتبّع كل ملف بجدول `_migrations` ويتخطى المُنفَّذ مسبقاً، ويعمل `ROLLBACK` كامل عند أي فشل — إعادة التشغيل آمنة عملياً رغم عدم idempotency الملف بمعزل عن السكربت

**تحقق فعلي**: `npx tsc --noEmit` نظيف، `npx vitest run` → **269/269 ناجح** (كانت 261 بنهاية v32؛ 8 اختبارات جديدة تغطي التصحيحين تحديداً).

## v34 — تنفيذ البند المفتوح صراحةً بـ§v31-8: مراجعة IDOR عبر foreign keys في procurement

v31 وثّقت أن نمط IDOR المُصلَح بـ`createDistribution` (تحقق ملكية السجلات المرتبطة، لا فقط `organizationId` المُرسَل صراحة) **لم يُراجَع** بعد بموديولات أخرى، وسمّت `procurement` صراحة (`vendorId/POid`) كأولوية. راجعنا `procurement/actions.ts` بالكامل ضد هذا النمط تحديداً ووجدنا ثغرة حقيقية:

### IDOR حقيقي في `checkBudgetCeiling` + `createPurchaseRequest` — **تم الإصلاح**
`checkBudgetCeiling(budgetLineId, amount)` كانت تجلب بند الميزانية بمعرّفه فقط، **بدون أي تحقق أنه يخص المنظمة المستدعية**. الأثر الفعلي مضاعف:
1. **تسريب بيانات مالية بين المستأجرين**: مستخدم من منظمة A يقدر يمرّر `budgetLineId` يخص منظمة B ضمن `createPurchaseRequest`، فترجع الدالة `available` (الرصيد المتاح الفعلي لبند ميزانية B) ضمن رسالة الخطأ لو تجاوز المبلغ المطلوب — تسريب مباشر لأرقام مالية لا يفترض أن يراها.
2. **الأخطر — تلوّث بيانات مالية بين المستأجرين**: `approvePurchaseRequest` تستدعي نفس الدالة للتحقق ثم، لو نجحت، **تُحدّث `committedAmount` على نفس `budgetLineId` مباشرة** (`grantBudgetLines` عبر `sql\`committed_amount + ...\``) بلا أي فحص ملكية إضافي. أي أن مستخدماً من منظمة A كان يقدر فعلياً **يغيّر** رقم الميزانية الملتزمة لمنظمة B بمجرد تخمين/معرفة UUID بند ميزانية تابع لها — ليس تسريب قراءة فقط، بل كتابة عبر حدود المستأجرين.

بالتوازي، `grantId` في `createPurchaseRequest` كان يُدرَج مباشرة بسجل `purchaseRequests` دون أي تحقق من ملكيته للمنظمة — يسمح بربط طلب شراء بمنحة منظمة أخرى (تلوّث تقارير مالية حسب المنحة/المانح).

**الإصلاح**:
- `checkBudgetCeiling` (`src/modules/grants/actions.ts`) صارت تأخذ `organizationId` إلزامياً كأول معامل، وترفض أي بند لا يطابق (`line.organizationId !== organizationId`) بنفس رسالة "غير موجود" تماماً — لا تمييز بين "غير موجود" و"يخص منظمة أخرى"، لمنع استكشاف وجود بنود منظمات أخرى عبر رسائل الخطأ (enumeration). كلا مستدعييها بـ`procurement/actions.ts` (`createPurchaseRequest`, `approvePurchaseRequest`) عُدِّلا لتمرير `organizationId`.
- `createPurchaseRequest` تتحقق الآن صراحة من `grants.organizationId` قبل أي كتابة لو أُرسل `grantId`، بنفس نمط `createDistribution` تماماً (`Promise.all` غير مطلوب هنا لأنه فحص واحد فقط، لكن نفس أسلوب "جلب ثم مقارنة `organizationId === v.data.organizationId`").

**لماذا لم تُكتشف سابقاً رغم مراجعتين خارجيتين (v31/v32)**: كلا المراجعتين ركّزتا على المخزون والمحاسبة (double-entry, race conditions, tamper-evidence) — لا أحد منهما فحص تدفق `budgetLineId`/`grantId` عبر موديول `procurement` تحديداً رغم أن v31 نفسها نبّهت لهذا كبند مفتوح صريح. هذا تذكير أن "البنود المفتوحة الموثّقة بصراحة" ليست مجرد شكليات — هذه المرة كانت تحتوي فعلاً على أخطر ثغرة اكتُشفت حتى الآن (كتابة مالية عبر حدود مستأجرين، لا مجرد قراءة).

**الحد المتبقي (لم يُراجَع بعد، بصراحة)**: نفس النمط لم يُراجَع منهجياً بعد فيما تبقى من procurement (`vendorId` — لا توجد دالة `createPurchaseOrder` بالكود أصلاً حالياً، أمر الشراء لا يُنشأ من أي مسار موجود رغم أن `approvePurchaseRequest` معلَّقة بتعليق "Approve PR → Create PO" ولا تنشئ PO فعلياً — هذا نقص وظيفي منفصل غير أمني، يستحق تنبيهاً)، ولا في `hr` (`employeeId`) أو `fleet` (`vehicleId`) كما نبّهت v31 أصلاً. الأولوية التالية المقترحة: نفس المراجعة على `vendors/actions.ts` (PO/تقييم عروض) و`hr/actions.ts` (رغم أن `budgetLineId` هناك مُستمدة من عقود موظفين مُصفّاة أصلاً بالمنظمة عبر السيرفر لا من مُدخَل عميل مباشر — أقل خطورة لكن تستحق تأكيداً صريحاً بمراجعة قادمة).

**تحقق فعلي**: تثبيت `npm install` كامل + `npx tsc --noEmit` نظيف + `npx vitest run` → **273/273 ناجح** (كانت 269 بنهاية v33؛ 4 اختبارات جديدة: رفض بند ميزانية/منحة تابعين لمنظمة أخرى، ونجاح الحالة الطبيعية).

## v34-تتمة — تنفيذ فعلي للمراجعة الموعودة على vendors/hr/fleet (بدل تأجيلها كبند مفتوح)

بعد سؤال مباشر: "لماذا لم تفحص باقي الموديولات؟" — تمت مراجعة الثلاثة كاملة سطراً بسطر (مو وصف عام). النتيجة: **أخطر ثغرة اكتُشفت بكل جولات المراجعة (v31 حتى الآن) موجودة بـhr، مو procurement**.

### 🔴 حرجة — `processPayroll` (hr/actions.ts): تسريب/تلوّث رواتب عبر كل المنظمات — **تم الإصلاح**
```ts
const activeContracts = await db.query.contracts.findMany({
  where: and(eq(contracts.isCurrent, true), eq(contracts.isArchived, false)),
  // ← لا يوجد فلتر organizationId إطلاقاً، رغم وجود العمود بالجدول
});
```
الأثر الفعلي: تشغيل رواتب لمنظمة A كان **يجلب عقود كل الموظفين بكل المنظمات على النظام**، يحسب رواتبهم، يُدرجها ضمن نفس كشف رواتب منظمة A، **ويخصم من `grantBudgetLines.spentAmount` لبنود ميزانية تابعة لمنظمات أخرى**. نفس الشيء بالضبط لاستعلام الحضور (`attendance` بدون فلتر منظمة). هذا أوسع بكثير من ثغرة procurement (v34 الأولى): تلك كانت تتطلب من المهاجم معرفة/تخمين UUID محدد؛ هذه كانت **تُشغَّل تلقائياً بكل مرة يعالج فيها أي مستخدم أي دورة رواتب لأي منظمة** — بلا حاجة لأي إدخال خبيث.

**الإصلاح**: إضافة `eq(contracts.organizationId, organizationId)` و `eq(attendance.organizationId, organizationId)` للاستعلامين، و`eq(grantBudgetLines.organizationId, organizationId)` لتحديث `spentAmount` (دفاع بعمق، رغم أن فلترة العقود تمنع الوصول لبند تابع لمنظمة أخرى أصلاً بعد الإصلاح الأول).

**لماذا لم تُكتشف رغم 3+ جولات مراجعة سابقة**: v31/v32/v33 ركّزت التدقيق على accounting/inventory (double-entry, race conditions, valuation). لا أحد منها فتح `hr/actions.ts` إطلاقاً حتى هذه الجولة. هذا أهم درس من هذه الجولة: التغطية الحقيقية تحتاج المرور على كل موديول يكتب بيانات مالية، مو الافتراض أن "بند مفتوح واحد موثّق" يعني الباقي سليم.

### 🟠 IDOR حقيقي — `submitVendorRating` (vendors/actions.ts) — **تم الإصلاح**
`vendorId` مُرسَل من العميل ويُدرَج بجدول `vendorRatings` دون تحقق ملكية. تحديث `vendors.overallScore` اللاحق كان آمناً (WHERE يتضمن organizationId، فيصبح no-op)، لكن **الإدراج نفسه** كان ينجح دائماً — تلوّث تقييمات موردين عبر حدود المستأجرين. تمت إضافة تحقق `vendor.organizationId !== data.organizationId` قبل أي كتابة.

### 🟠 IDOR حقيقي — `createFuelLog` و`createMaintenanceRecord` (fleet/actions.ts) — **تم الإصلاح**
كلتاهما كانت تُدرج سجلاً (وقود/صيانة) بـ`vehicleId` مُرسَل من العميل **دون أي تحقق ملكية** — لا حتى فحص `vendor غير موجود`. تمت إضافة `db.query.vehicles.findFirst` + تحقق `organizationId` قبل الإدراج بكلتا الدالتين.

### 🟡 IDOR أقل حدة — `assignDriver` و`createTrip` (fleet/actions.ts) — **تم الإصلاح**
`driverId` (بكلتا الدالتين) و`grantId` (بـ`createTrip`) كانا يُدرجان/يُحدَّثان دون تحقق ملكية — نفس نمط `grantId` بـprocurement. تمت إضافة نفس التحقق.

### الخلاصة الرقمية
- ملفات مفحوصة سطراً بسطر هذه الجولة: `vendors/actions.ts` (262 سطر)، `hr/actions.ts` (568 سطر)، `fleet/actions.ts` (222 سطر) — كاملة، لا عيّنات.
- 6 دوال أُصلحت عبر 3 ملفات.
- اختبارات جديدة: 11 (hr: 1 حرج + توسيع mocks، fleet: 8، vendors: 2).

**تحقق فعلي**: `npx tsc --noEmit` نظيف، `npx vitest run` → **284/284 ناجح** (كانت 273 قبل هذه الجولة).

### الحد المتبقي — بصراحة، لم يُغلَق بعد
1. **`createPurchaseOrder` غير موجودة إطلاقاً** بالكود — تحقّقتُ فعلياً بـ`grep -rn "purchaseOrders).*insert\|insert(purchaseOrders"` على كامل `src/modules` ولا نتيجة. `approvePurchaseRequest` معلَّقة بتعليق "Approve PR → Create PO" لكنها لا تُنشئ PO. `createGoodsReceiptNote` تفترض وجود PO مسبق (`db.query.purchaseOrders.findFirst`) لكن لا يوجد أي مسار بالكود ينشئ واحداً. **هذا نقص وظيفي (feature gap)، ليس ثغرة أمنية** — لكنه يعني أن تدفق PR→PO→GRN غير قابل للتشغيل فعلياً بالوضع الحالي. يستحق أولوية لو كانت الخطوة القادمة تفعيل هذا التدفق.
2. `submitBidScore` (vendors): `relatedPoId`/`relatedGrnId` بـ`ratingSchema` غير مُتحقَّق ملكيتهما (لكنهما حقلان اختياريان يُخزَّنان فقط كمرجع، لا يُستخدَمان لاحقاً بأي كتابة مالية أو حسبة — خطورة منخفضة جداً، لم تُصلَح بهذه الجولة لضيق الأثر مقابل الوقت).
3. `flagEmergencyProcurement` (vendors): تُنشئ سجل تدقيق (`createAuditLog`) حتى لو كان تحديث `purchaseRequests` no-op فعلياً (PR تابع لمنظمة أخرى) — يُنتج سجل تدقيق مضلِّل لعملية لم تحدث فعلياً. تسريب معلومات محدود جداً (لا بيانات مالية)، لم يُصلَح بهذه الجولة.
4. لم تتم مراجعة موديولات أخرى خارج القائمة المطلوبة صراحة هذه الجولة (`beneficiaries` سبق إصلاحها بنمط مشابه بجولة أقدم — راجع `createDistribution`؛ لم تُعاد مراجعتها هنا).

## v34-تتمة-2 — إغلاق البنود الثلاثة المتبقية أعلاه + نقص وظيفي حرج + race condition بـpayroll

بعد سؤال مباشر "ماذا بقي؟!" ثم "تابع كل شيء" — تم تنفيذ كل بند كان موثّقاً كمفتوح أعلاه، بدل تركه كملاحظة.

### ✅ `submitBidScore` (vendors): `relatedPoId`/`relatedGrnId` — تم الإصلاح
تحقق ملكية صريح لكلا الحقلين الاختياريين قبل أي كتابة، بنفس نمط بقية الإصلاحات.

### ✅ `flagEmergencyProcurement` (vendors): سجل تدقيق مضلِّل — تم الإصلاح
صار يتحقق من وجود/ملكية الـPR (`db.query.purchaseRequests.findFirst` + مقارنة `organizationId`) **قبل** أي تحديث أو تسجيل تدقيق. PR غير موجود أو تابع لمنظمة أخرى → رفض فوري، لا كتابة DB إطلاقاً (لا تحديث ولا سجل تدقيق).

### ✅ `createPurchaseOrder` (procurement) — نقص وظيفي حرج، بُني من الصفر
كان غائباً تماماً؛ الآن موجود بالكامل مع: تحقق أن الـPR معتمد (`status === "approved"`) قبل السماح، تحقق ملكية `vendorId`، تحقق ملكية أي `prItemId` مرتبط ببنود الـPR نفسه، توريث `grantId`/`budgetLineId` من الـPR المعتمد (**لا تُقبَل من العميل مباشرة** — تجنباً لنفس ثغرة IDOR بـ`grantId` المُصلَحة سابقاً بـ`createPurchaseRequest`)، حماية idempotency، ونقل الـPR لحالة `done` بعد الإصدار. 8 اختبارات جديدة (`po-actions.test.ts`).

### ✅ Race condition حقيقي جديد — `processPayroll` (hr) — تم الإصلاح
فحص "كشف رواتب مكرر لنفس الفترة" كان قراءة-ثم-كتابة (check-then-act) بلا قفل أو قيد DB — نفس فئة ثغرة المخزون المُصلَحة بـv31 #1، لكن بموديول الرواتب: طلبان متزامنان لنفس (organizationId, month, year) كانا يقدران يمرّان الفحص معاً وينتجان كشفي رواتب مكررين، يضاعف صرف الرواتب فعلياً ويخصم مرتين من ميزانيات المنح. **الإصلاح**: migration جديدة `015_payroll_run_uniqueness.sql` تضيف `UNIQUE INDEX` حقيقي على `(organization_id, month, year)`، مع ترجمة خطأ postgres `23505` لرسالة عربية ودّية بدل تسريبه خاماً للمستخدم.

### ✅ Pagination — 4 صفحات قوائم حقيقية إضافية أُصلحت
فحص كامل لتوزيع "55 استدعاء findMany" الموثّق كبند مفتوح بv32 كشف أن أغلبها إما صفحات تفاصيل (`[id]/page.tsx`، مقيّدة أصلاً بسجل واحد) أو قوائم منسدلة صغيرة الحجم بطبيعتها (شجرة حسابات، موظفون نشطون لقائمة اختيار). فُرزت 4 صفحات قوائم حقيقية غير محمية سابقاً وأُصلحت بنفس نمط `parsePagination`/`PaginationBar` المستخدم بـ vendors/grants/procurement/fleet:
- `hr/page.tsx` (قائمة الموظفين الكاملة)
- `cfm/page.tsx` (الشكاوى) — مع الحفاظ على دقة الإحصائيات (مفتوحة/متجاوزة/حساسة) عبر استعلام أعمدة خفيف منفصل يغطي كل السجلات، بدل فقدان الدقة أو جلب كل الأعمدة الكاملة لكل السجلات فقط لحساب عدّاد.
- `inventory/page.tsx` (الأصناف + الأصول الثابتة) — يجلب الآن تبويب واحد نشط فقط (لا كليهما معاً)، مع عدّادين إجماليين منفصلين خفيفين لكل تبويب تبقى دقيقة بغض النظر عن التبويب المعروض.
- `partners/page.tsx` (الشركاء المنفّذون)

**لم تُفحَص/تُصلَح**: باقي الـ 55 استدعاء — أغلبها صفحات تفاصيل بمعرّف واحد (خطورة منخفضة جداً بطبيعتها)، لم تُراجَع فرداً فرداً لتأكيد ذلك 100%، فقط بالفرز العام حسب اسم المسار (`[id]/page.tsx` مقابل `page.tsx` مباشرة).

### محاولة حقيقية فشلت — اختبار تكامل ضد Postgres حقيقي (v31 #6، ما زال مفتوحاً)
حاولت فعلياً `apt-get install postgresql` بهذا الـsandbox لعمل اختبار تكامل حقيقي بعدة اتصالات فعلية (البند المتبقي الوحيد من v31 الذي يحتاج بنية تحتية لا كوداً فقط). فشل: مرآة `security.ubuntu.com` ترجع 404 لكل حزم postgres عبر الـproxy المتاح بهذه البيئة. هذا قيد بيئة حقيقي غير قابل للحل من داخل هذه الجلسة — القيد `UNIQUE INDEX` وCHECK constraints المضافة بmigrations 013-015 صحيحة بنيوياً (تحققت من صياغة SQL يدوياً) لكن لم تُختبَر فعلياً ضد Postgres حقيقي متعدد الاتصالات بهذه الجلسة تحديداً.

**تحقق فعلي**: `npx tsc --noEmit` نظيف (يشمل صفحات pagination الأربع)، `npx vitest run` → **298/298 ناجح** (بلا تغيير عددي عن الجولة السابقة لأن إصلاحات pagination بصفحات `page.tsx` غير مغطاة باختبارات unit بهذا المشروع أصلاً — لا توجد بنية اختبار Server Components هنا).

### ما تبقى مفتوحاً فعلياً بعد كل هذه الجولات (بصراحة كاملة، بلا مبالغة)
1. اختبار تكامل حقيقي ضد Postgres متعدد الاتصالات — يحتاج بيئة CI بشبكة وصول لمرايا apt تعمل، غير متاح بهذا الـsandbox.
2. Audit log immutability الكاملة (دور DB منفصل بصلاحيات REVOKE، أو HMAC بمفتاح KMS خارجي) — قرار بنية تحتية خارج نطاق الكود، غير مطبَّق.
3. الرصيد الإهلاكي بالمخزون (`items.currentStock`): لا تسوية دورية مجدولة (cron/scheduled job) بين الـcache والـledger الحقيقي.
4. Idempotency غير مربوطة بكل عملية حساسة — `processPayroll` الآن محمي بقيد DB مباشر (مو idempotencyKeys)، وهذا كافٍ لمنعه لكنه نمط مختلف عن آلية idempotencyKeys الموحّدة المستخدمة بـ`createPurchaseOrder`/`createGoodsReceiptNote`. لم يُوحَّد النمطان.
5. باقي ~51 استدعاء `findMany` لم تُفحَص فرداً فرداً (فُرزت تقديرياً فقط حسب اسم المسار).
6. `beneficiaries` وموديولات أخرى غير الأربعة المفحوصة هذه الجولات (procurement, hr, vendors, fleet) لم تُعَد مراجعتها بنفس عمق IDOR للتأكد من عدم وجود نمط مشابه.

## v35 — تدقيق أمني شامل (Production Readiness Verification) عبر كل الموديولات

طلب مباشر بإجراء فحص جذري شامل (لا عيّنات) لخمسة محاور: IDOR/multi-tenancy عبر كل الموديولات المتبقية، Race Conditions بكل عمليات "اقرأ-ثم-اكتب" المالية/المخزنية، قيود قاعدة بيانات (CHECK/Triggers)، تكامل دفتر المخزون مع المحاسبة، وتوحيد Idempotency/Audit. راجعنا كل الموديولات المتبقية (accounting, beneficiaries, cfm, hr, partners, procurement, grants) سطراً بسطر ضد الأنماط الموثّقة أعلاه — النتيجة: **7 ثغرات حقيقية جديدة** (لا شيء منها كان موثّقاً بالجولات السابقة)، بالإضافة لاكتشاف تشغيلي خطير: **migration 015 (الموثّق أعلاه بv34-تتمة-2 كـ"تم الإصلاح") لم يكن مُسجَّلاً إطلاقاً بقائمة `run-migrations.ts`** — أي أن قيد `UNIQUE INDEX` لمنع تكرار كشوف الرواتب لم يصل فعلياً لأي قاعدة بيانات حقيقية رغم توثيقه كمُغلَق. تم تسجيله الآن.

### 🔴 IDOR حقيقي — `createJournalEntryInner` و`reverseJournalEntry` (accounting/actions.ts) — **تم الإصلاح**
`fiscalPeriodId` (بكلتا الدالتين، عبر `targetFiscalPeriodId` بالعكس) و`accountId` (بكل سطر قيد) كانا يُجلَبان/يُدرَجان بلا أي تحقق ملكية للمنظمة. الأثر: مستخدم من منظمة A يقدر ينشئ/يعكس قيداً داخل فترة محاسبية تابعة لمنظمة B، أو يحرّك رصيد حساب (`accounts.currentBalance`) تابع لمنظمة B فعلياً عند الترحيل — نفس فئة ثغرة `budgetLineId` بv34 لكن بموديول المحاسبة الأساسي الذي لم يُفحص من قبل بهذا العمق. **الإصلاح**: تحقق صريح من `fiscalPeriods.organizationId` و`accounts.organizationId` (دفعة واحدة عبر `inArray` لكل الحسابات المستخدَمة) قبل أي كتابة، بنفس رسالة "غير موجود" لمنع enumeration.

### 🔴 Race Condition حقيقي — التزام ميزانية طلبات الشراء (`approvePurchaseRequest`) — **تم الإصلاح**
`checkBudgetCeiling` قراءة فقط، والكتابة الفعلية (`committedAmount +=`) منفصلة زمنياً بلا أي قفل أو شرط ذرّي — نفس فئة ثغرة المخزون (v31) والرواتب (v34-تتمة-2)، لكن هذه لم تُصلَح رغم أن v34 وثّقت اكتشاف مشكلة *قراءة* مشابهة بنفس الدالة (تسريب/كتابة IDOR) دون ملاحظة أن الكتابة نفسها غير ذرّية أيضاً. طلبا شراء متزامنان على نفس بند الميزانية يقدران يتجاوزان السقف فعلياً. **الإصلاح**: `commitBudgetAmountAtomic()` جديدة بـgrants/actions.ts — `UPDATE...WHERE planned - committed - spent >= amount` بعملية SQL واحدة، مستدعاة داخل نفس transaction تحديث حالة الـPR (rollback كامل لو رُفض الالتزام).

### 🟠 Race Condition حقيقي — استلام البضاعة (`createGoodsReceiptNote`) — **تم الإصلاح**
فحص "لا يتجاوز الكمية المطلوبة" كان قراءة (`findMany`) ثم مقارنة JS ثم `UPDATE` منفصل زمنياً — سندا استلام متزامنان لنفس بند أمر الشراء يقدران يمرّان الفحص معاً ويُنتجان استلاماً تراكمياً يتجاوز المطلوب فعلياً (يُدخِل مخزوناً/مصروفات غير مطلوبة). **الإصلاح**: `UPDATE purchaseOrderItems ... WHERE received_qty + x <= quantity` ذرّي، مع فحص `rowCount` (0 = رُفض).

### 🟠 Race Condition + نقص Transaction — صرف المنح الفرعية (`createDisbursement`, partners/actions.ts) — **تم الإصلاح**
فحص "لا يتجاوز الرصيد المتبقي" قراءة منفصلة عن **INSERT وUPDATE غير مُغلَّفين حتى بمعاملة tx واحدة** — بخلاف كل العمليات المالية الأخرى بالمشروع. أثران: (1) نفس فئة Race Condition (صرفان متزامنان يتجاوزان إجمالي المنحة الفرعية)، (2) لو فشل الـUPDATE بعد نجاح الـINSERT، يبقى سند صرف "مكتمل" مسجَّلاً دون أي انعكاس على رصيد المنحة (لا rollback). **الإصلاح**: كل شي بمعاملة واحدة، والتحديث ذرّي (`WHERE total - disbursed >= amount`).

### 🟡 IDOR — إدراج سجل فرعي بلا تحقق ملكية الأصل (نمط متكرر بـ3 دوال) — **تم الإصلاح**
نفس النمط بالضبط بثلاث دوال لم تُفحص من قبل: `addCaseNote` (beneficiaries)، `addComplaintUpdate` (cfm)، `addPayrollAdjustment` (hr) — كل واحدة تستقبل معرّف سجل أب (`caseId`/`complaintId`/`employeeId`) مع `organizationId`، وتتحقق فقط من مطابقة `organizationId` لجلسة المستخدم، لا من أن السجل الأب نفسه يخص هذه المنظمة فعلياً. **الإصلاح**: تحقق `findFirst` + مقارنة `organizationId` قبل أي إدراج بالثلاثة، بنفس النمط المعتمد بكل الإصلاحات السابقة.

### 🆕 CHECK Constraints جديدة (migration 016) — دفاع أخير على مستوى القاعدة
إضافة لكل الإصلاحات أعلاه على مستوى التطبيق، أُضيفت قيود CHECK تمنع نفس الحالات حتى لو تجاوزها كود مستقبلي لا يستخدم الدوال الذرّية الجديدة:
- `grant_budget_lines`: `committed_amount + spent_amount <= planned_amount`، وكلاهما `>= 0`
- `sub_grants`: `disbursed_amount <= total_amount`، و`>= 0`
- `purchase_order_items`: `received_qty <= quantity` (يسمح NULL)، و`>= 0`

**⚠️ ملاحظة نشر مهمة موثّقة داخل الملف نفسه**: `ADD CONSTRAINT CHECK` يفحص كل الصفوف الموجودة وقت التنفيذ ويفشل لو وُجدت بيانات مخالفة فعلاً (محتمل جداً هنا بما أن الثغرات وُثِّقت كموجودة سابقاً) — يجب تشغيل الاستعلامات التشخيصية المُرفَقة بالملف والتحقق من نظافة البيانات، أو استخدام `NOT VALID` + `VALIDATE CONSTRAINT` لاحقاً، قبل التنفيذ على أي بيئة فيها بيانات فعلية.

### تحقق فعلي
`npx tsc --noEmit` نظيف، `npx vitest run` → **313/313 ناجح** (كانت 298 بنهاية v34-تتمة-2؛ 15 اختباراً جديداً تغطي الإصلاحات السبعة أعلاه، ومُعدَّلة اختبارات موجودة سابقاً لتوافق شكل الاستدعاءات الذرّية الجديدة دون تغيير سلوكها المُختبَر). لم يُشغَّل بعد ضد Postgres حقيقي (نفس القيد البيئي الموثّق بكل الجولات السابقة).

### ما تبقى مفتوحاً فعلياً بعد v35 (بصراحة كاملة)
1. نفس بنود v34-تتمة-2 المفتوحة (integration test حقيقي، audit log immutability الكاملة، تسوية دورية لـ`items.currentStock`، ~51 استدعاء `findMany` غير مُراجَع فرداً فرداً).
2. **جديد**: migration 016 لم يُختبَر بعد ضد بيانات فعلية — يحتاج تشغيل الاستعلامات التشخيصية أولاً (موثّق بالملف نفسه) قبل أي نشر إنتاجي.
3. `submitVendorRating` (vendors) و`submitBidScore` يعيدان حساب متوسط تراكمي (average) بقراءة كل السجلات بعد كل إدراج بلا قفل — تحت تزامن حقيقي يُنتج نتيجة نهائية قد تفوّت آخر تحديث (last-write-wins على رقم تراكمي)، لكن الأثر تجميلي (متوسط تقييم/درجة عرض) لا مالي مباشر — لم يُصلَح بهذه الجولة لضيق الأثر مقابل الأولوية.
4. لم تُفحص بعد بنفس العمق: `donor-reports`, `gis`, `screening` (فُحصت جزئياً فقط بجولات سابقة)، وموديولات القراءة الصرفة (`reports`).


## v36 — إغلاق النقاط المتبقية (donor-reports, gis, screening, reports) + Idempotency + Pagination + Audit Integrity

طلب مباشر بإغلاق خمس نقاط متبقية من v35: (1) تدقيق IDOR على الموديولات الأربعة غير المفحوصة (donor-reports, gis-mapping, screening, reports)، (2) توحيد idempotency على كل الإجراءات المالية، (3) فرز نهائي لـ~51 استدعاء findMany، (4) حماية سلامة سجل التدقيق، (5) تقييم ذاتي نهائي.

### 1) donor-reports / gis-mapping / reports — **لا ثغرات وُجدت**
فُحصت الصفحات الأربع (`reports/page.tsx`, `reports/donor/[grantId]/page.tsx`, `reports/gis-map/page.tsx`, `reports/budget-vs-actual/page.tsx`) سطراً بسطر: كل استعلام مُقيَّد بـ`organizationId` من الجلسة، `donor/[grantId]` تتحقق صراحة `grant.organizationId !== orgId → notFound()` قبل أي استخدام. لا IDOR، لا استعلامات غير محدودة حقيقية (هذه صفحات تجميع/تقارير داخلية، ليست قوائم متصفَّحة).

### 🔴 IDOR حقيقي — `recordScreening` (screening/actions.ts) — **تم الإصلاح**
كانت تتحقق فقط من مطابقة `organizationId` المُرسَل، بلا أي تحقق أن `entityId` (شريك/مورد/مستفيد/موظف) فعلاً يخص هذه المنظمة — نفس نمط الثغرات المُغلَقة سابقاً (v35 §8) لكن بموديول لم يُفحص بهذا العمق بعد. **الإصلاح**: تحقق ملكية صريح حسب `entityType` قبل الإدراج (partners/vendors/beneficiaries/employees)، برسالة فشل عامة موحّدة لمنع enumeration.

### 🔴 IDOR حقيقي إضافي — `createSubGrant` (partners/actions.ts) — **تم الإصلاح**
اكتُشفت أثناء إضافة idempotency لنفس الدالة: `partnerId` و`parentGrantId` كانا يُدرَجان بلا أي تحقق ملكية للمنظمة (فحص الشريك كان يتحقق فقط من وجوده وحالة الفحص المسبق، لا من `organizationId`). **الإصلاح**: تحقق صريح على كلا الحقلين قبل الإدراج.

### Idempotency — تم توحيده على `createSubGrant` و`createDisbursement` (partners/actions.ts)
كانت `createDisbursement` بلا أي حماية idempotency رغم كونها عملية مالية تنشئ سند صرف فعلي — نقرة مزدوجة/retry كان يُنتج صرفين منفصلين طالما كل واحد ضمن الرصيد المتبقي (القيد الذرّي بv35 يمنع فقط تجاوز السقف الإجمالي، لا التكرار نفسه). **الإصلاح**: كلتاهما ملفوفتان الآن بـ`withIdempotency` بنفس نمط `createPurchaseOrder`/`createGoodsReceiptNote`/`createDistribution`.
**الحد المتبقي الموثّق (v35 بند 4)**: `processPayroll` ما زالت تعتمد على قيد UNIQUE مباشر بقاعدة البيانات بدل نمط `idempotencyKeys` الموحّد — نمطان مختلفان يحققان نفس الهدف، لم يُوحَّدا بهذه الجولة (لا خطر فعلي، فرق تصميم فقط).

### Pagination / Unbounded Queries — فُحص كل استدعاء (97) فردياً، لا بالتقدير
- **حرج**: `notifyMany` (core/notifications/notify.ts) كانت تجلب **كل مستخدمي النظام عبر كل المنظمات بلا أي `where`** ثم تُصفّي بـJS — استعلام غير محدود فعلياً ينمو مع كل مستخدم جديد بأي منظمة، يُستدعى من كل تنبيه جماعي (تحذيرات ميزانية، إلخ). **أُصلح**: `inArray(users.id, userIds)`.
- `settings/users/page.tsx`: بلا أي `limit` إطلاقاً. أُضيف سقف دفاعي `limit:500`.
- `beneficiaries/page.tsx`: مُقيَّدة بـ`limit:200` بلا pagination حقيقي — **والإحصائيات المعروضة (قيد المراجعة/موثّق/ازدواجية) كانت خاطئة فعلياً لأي منظمة تتجاوز 200 مستفيد** لأنها تُحسب فقط من الـ200 المعروضة. أُصلحت بنفس نمط cfm/inventory: أعمدة خفيفة منفصلة للإحصاء الدقيق + `parsePagination`/`PaginationBar` للقائمة.
- `accounting/page.tsx` (قيود اليومية): نفس النمط — `limit:50` ثابت بلا صفحة تالية، أي قيد أقدم من الخمسين الأحدث كان غير قابل للوصول. أُصلحت بنفس نمط pagination الموحّد.
- باقي الاستدعاءات (~92 من 97): فُحصت فرداً فرداً وتبيّن أنها مقيَّدة أصلاً بمفتاح عمل طبيعي (سجل واحد، منحة واحدة، قائمة منسدلة صغيرة، `limit` صريح معقول) أو صفحات قوائم لها pagination فعلي مسبق (vendors/grants/procurement/fleet/hr/cfm/inventory/partners — 8 صفحات، جميعها مؤكَّدة تستخدم `parsePagination`).

### Audit Log Integrity
**لا تعديل ممكن من منطق التطبيق**: بحث شامل عن `auditLogs` عبر الكود بأكمله يؤكد عدم وجود أي `update`/`delete` عليها إطلاقاً — فقط `insert` (عبر `createAuditLog`) و`findFirst`/`findMany` للقراءة والتحقق (`verifyAuditChain`). الحد البنيوي الموثّق سابقاً (v35 §7 — DB owner يقدر يعدّل، يحتاج REVOKE/HMAC/WORM خارج نطاق الكود) يبقى كما هو، بلا تغيير.

**فجوات تغطية حقيقية (لا فشل صامت، بل غياب كامل) — أُغلقت 8 حالات**:
- `fleet/actions.ts`: **6 إجراءات كانت تكتب بلا أي تسجيل تدقيق إطلاقاً**: `createDriver`, `assignDriver`, `completeTrip`, `createFuelLog`, `createMaintenanceRecord`, `setVehicleStatus`. اثنان منها ماليان مباشرة (`createFuelLog`, `createMaintenanceRecord` — يحملان `cost` فعلي).
- `cfm/actions.ts` → `addComplaintUpdate`: إدراج بلا تدقيق.
- `beneficiaries/actions.ts` → `addCaseNote`: إدراج بلا تدقيق.
- `vendors/actions.ts` → `setupBidCriteria`, `submitBidScore`: تؤثران مباشرة على قرار ترسية عطاءات (تقييم/معايير المناقصة) وكانتا بلا أي تسجيل تدقيق.

كل الثمانية أُصلحت بإضافة `createAuditLog` بنفس نمط باقي الموديولات.

### تحقق فعلي
`npx tsc --noEmit` → نظيف بالكامل.
`npx vitest run` → **317/317 ناجح** (كانت 313 بنهاية v35؛ 4 اختبارات جديدة: اختباران لثغرة IDOR بـ`recordScreening` [كيان تابع لمنظمة أخرى / كيان غير موجود]، واختباران لثغرة IDOR بـ`createSubGrant` [شريك تابع لمنظمة أخرى / منحة أب تابعة لمنظمة أخرى]. الاختبارات الموجودة سابقاً بـ`screening` و`partners` عُدِّلت لتوفير mocks لاستعلامات التحقق الجديدة (`db.query.partners/vendors/beneficiaries/employees.findFirst`, `db.query.grants.findFirst`) دون تغيير سلوكها المُختبَر الأصلي).

### الملفات المعدَّلة بهذه الجولة
- `src/modules/screening/actions.ts` — إصلاح IDOR (تحقق ملكية الكيان المُشار إليه)
- `src/modules/partners/actions.ts` — إصلاح IDOR (`createSubGrant`) + idempotency (`createSubGrant`, `createDisbursement`)
- `src/modules/fleet/actions.ts` — إضافة audit logging لـ6 إجراءات
- `src/modules/cfm/actions.ts` — إضافة audit logging لـ`addComplaintUpdate`
- `src/modules/beneficiaries/actions.ts` — إضافة audit logging لـ`addCaseNote`
- `src/modules/vendors/actions.ts` — إضافة audit logging لـ`setupBidCriteria`, `submitBidScore`
- `src/core/notifications/notify.ts` — إصلاح استعلام غير محدود حرج (`notifyMany`)
- `src/app/(dashboard)/settings/users/page.tsx` — إضافة سقف دفاعي
- `src/app/(dashboard)/beneficiaries/page.tsx` — pagination حقيقي + إصلاح إحصائيات خاطئة
- `src/app/(dashboard)/accounting/page.tsx` — pagination حقيقي لقيود اليومية
- اختبارات معدَّلة/مضافة: `screening/__tests__/actions.test.ts`, `partners/__tests__/actions.test.ts`, `beneficiaries/__tests__/actions.test.ts`

### ما تبقى مفتوحاً فعلياً بعد v36 (بصراحة كاملة، بلا مبالغة)
1. اختبار تكامل حقيقي ضد Postgres متعدد الاتصالات — نفس القيد البيئي من v31 إلى الآن (لا وصول لمرايا apt بهذا الـsandbox)، غير محلول.
2. Audit log immutability الكاملة (DB role منفصل REVOKE، أو HMAC/KMS، أو WORM خارجي) — قرار بنية تحتية خارج نطاق الكود، كما هو موثّق منذ v31.
3. تسوية دورية مجدولة بين `items.currentStock` (cache) و`stock_ledger` (المصدر الحقيقي) — لم تُبرمج بعد.
4. `submitVendorRating`/`submitBidScore` (vendors): متوسط تراكمي بقراءة كل السجلات بعد كل إدراج بلا قفل — أثر تجميلي (لا مالي مباشر)، لم يُصلَح لضيق الأولوية (نفس البند من v35).
5. `processPayroll`: نمط idempotency مختلف (قيد DB مباشر) عن نمط `idempotencyKeys` الموحّد المستخدم بباقي الإجراءات المالية — يعمل بشكل صحيح لكن غير موحّد الشكل.
6. migration 016 (CHECK constraints v35) لم تُختبَر بعد ضد بيانات فعلية بهذه الجولة — نفس الملاحظة من v35 تبقى سارية قبل أي نشر.

## v37 — تجهيز النشر الإنتاجي (Production Readiness)

طلب مباشر: تحضير صارم للنشر الفعلي دون إيقاف خدمة أو فشل migrations على
بيانات قديمة. لا إصلاحات ثغرات جديدة بهذه الجولة — بنية تحتية ونشر فقط.

### 1) سكريبت تشخيصي قبل migration 016 — **جديد**
`scripts/pre-migration-016-diagnostics.ts` (قراءة فقط، SELECT) يفحص
الجداول الثلاثة التي يحميها migration 016 (`grant_budget_lines`,
`sub_grants`, `purchase_order_items`) ويطبع عدد الصفوف المخالفة وعيّنة
منها. بند 6 من قائمة v36 المفتوحة ("لم تُختبَر بعد ضد بيانات فعلية") يبقى
**غير محلول من ناحية التنفيذ الفعلي** (لا وصول لقاعدة إنتاج حقيقية بهذا
الـsandbox، نفس القيد البيئي الموثّق من v31) — لكن أداة التشخيص وخطة
المعالجة (`scripts/016_budget_and_disbursement_guards_not_valid.sql`
بصيغة NOT VALID/VALIDATE CONSTRAINT) جاهزتان الآن لأي بيئة حقيقية.

### 2) دالة موحّدة للتحقق من ملكية المؤسسة — **جديد**
`src/lib/auth/ownership.ts` (`assertOwnedByOrg`, `assertAllOwnedByOrg`)
تعمل مع أي جدول فيه عمودا `id`/`organizationId` (كل الجداول التشغيلية
عبر `baseColumns`). طُبِّقت على نقطتي التحقق اللتين وُلِّدتا يدوياً
بأنماط مختلفة سابقاً: تحقق الكيان بـ`recordScreening` (screening/actions.ts)
وتحقق المنحة الأم بـ`createSubGrant` (partners/actions.ts). **لم تُطبَّق
بعد** على بقية نقاط التحقق اليدوي المشابهة بموديولات أخرى (مثل تحقق
partnerId بنفس `createSubGrant`، الذي يحتاج السجل كاملاً لأسباب أخرى
[dueDiligenceStatus] فلم يُستبدَل) — الهدف بهذه الجولة توفير النمط
الموحَّد ونشره بموديولين ملموسين، لا مراجعة شاملة لكل الكود مجدداً.

### 3) مهمة دورية لسلامة سجل التدقيق — **جديد**
`verifyAuditChain(organizationId)` كانت موجودة (v31) لكن بلا أي مسار
تشغيلي يستدعيها تلقائياً. أُضيف: `src/core/audit/scheduled-check.ts`
(يفحص كل المنظمات النشطة، ينبّه عبر مسارين مستقلين عند أي كسر — داخل
النظام لمسؤولي المنظمة + إيميل مباشر لـ`SECURITY_ALERT_EMAIL` خارج نظام
المستخدمين) و`scripts/verify-audit-integrity.ts` (نقطة تشغيل CLI/cron).
جُدولة أسبوعياً عبر Render Cron Job (`render.yaml`)، مع بديل GitHub
Actions موثّق بـ`DEPLOYMENT_GUIDE.md` لو لم يكن Render Cron متاحاً بالخطة
الحالية.

### 4) دليل النشر — **جديد**
`DEPLOYMENT_GUIDE.md` يوثّق ترتيب migrations، أوامر البناء، متغيرات
البيئة (بما فيها `SECURITY_ALERT_EMAIL` الجديد)، وضع KMS/الأسرار الحالي
(متغيرات بيئة عادية بلوحة المنصة — لا KMS خارجي؛ الحاجة الفعلية لواحد
مرتبطة بقرار immutability الكامل لسجل التدقيق الموثّق بـv31 §7، خارج
نطاق هذه الجولة)، وقائمة تفقد نهائية قبل الإعلان عن اكتمال النشر.

### تحقق فعلي
`npx tsc --noEmit` → نظيف بالكامل.
`npx vitest run` → **332/332 ناجح** (317 من v36 + 15 اختباراً جديداً:
8 لـ`ownership.ts`، 6 لـ`scheduled-check.ts`، وتعديل/إضافة اختبار واحد
بـ`screening/actions.test.ts` لتغطية تمرير الجدول الصحيح حسب نوع الكيان).

### الملفات الجديدة/المعدَّلة بهذه الجولة
- `src/lib/auth/ownership.ts` (جديد) + `__tests__/ownership.test.ts`
- `src/modules/screening/actions.ts` — استبدال تحقق الكيان بـ`assertOwnedByOrg`
- `src/modules/partners/actions.ts` — استبدال تحقق المنحة الأم بـ`assertOwnedByOrg`
- `src/modules/screening/__tests__/actions.test.ts`,
  `src/modules/partners/__tests__/actions.test.ts` — تحديث المحاكاة لتعكس ذلك
- `src/core/audit/scheduled-check.ts` (جديد) + `__tests__/scheduled-check.test.ts`
- `scripts/pre-migration-016-diagnostics.ts` (جديد)
- `scripts/016_budget_and_disbursement_guards_not_valid.sql` (جديد، بديل اختياري)
- `scripts/verify-audit-integrity.ts` (جديد)
- `scripts/check-env.ts` — إضافة `SECURITY_ALERT_EMAIL` كمتغيّر مُوصى به
- `package.json` — أوامر `check:migration-016` و`audit:verify`
- `render.yaml` — خدمة cron أسبوعية لـ`audit:verify`
- `.env.production.example` — `SECURITY_ALERT_EMAIL` + تحديث قائمة التفقد
- `DEPLOYMENT_GUIDE.md` (جديد)

### ما تبقى مفتوحاً فعلياً بعد v37 (بصراحة كاملة، بلا مبالغة)
1. كل بنود v36 المفتوحة أعلاه (integration test حقيقي، audit immutability الكاملة عبر KMS/REVOKE/WORM، تسوية `items.currentStock`، متوسط `submitVendorRating`/`submitBidScore` التراكمي، توحيد نمط idempotency بـ`processPayroll`) — بلا تغيير، لم تُلمَس بهذه الجولة (نطاقها بنية تحتية/تصميم، لا نشر).
2. migration 016 لم تُختبَر بعد فعلياً ضد بيانات إنتاج حقيقية — الأداة والخطة جاهزتان (بند 1 أعلاه)، لكن التنفيذ الفعلي ينتظر بيئة حقيقية لا يوفرها هذا الـsandbox.
3. `assertOwnedByOrg` عُمِّمت بv38 على كل حالة "جلب فقط للتحقق" (السجل لا يُستخدم إلا لمقارنة organizationId) عبر الموديولات العشرة المتبقية — راجع قسم v38 أدناه للتفاصيل والاستثناءات المتعمَّدة.
4. لا يوجد اختبار فعلي لتسليم بريد `SECURITY_ALERT_EMAIL` على مزوّد حقيقي (Resend/SMTP) — الاختبارات الحالية تُحاكي `sendEmail` بالكامل؛ التحقق الفعلي من وصول البريد يحتاج تشغيلاً يدوياً على بيئة حقيقية (موثّق بقائمة التفقد بـ`DEPLOYMENT_GUIDE.md`).
5. لا KMS/HSM خارجي مُطبَّق — قرار بنية تحتية مفتوح منذ v31، موثّق بوضوح بـ`DEPLOYMENT_GUIDE.md` §4 بدل تركه ضمنياً.

## v38 — تعميم `assertOwnedByOrg` على الموديولات المتبقية (تدقيق مستقل + تنفيذ)

بند v37 §3 المفتوح ("طُبِّقت على موديولين فقط كإثبات نمط") نُفِّذ الآن، لكن
**ليس عبر استبدال ميكانيكي شامل لكل الحالات الـ96 المرصودة**. بالمراجعة
تبيّن أن أغلبها "جلب-لمنطق-عمل" — السجل المجلوب للتحقق يُستخدم لاحقاً
لحقل آخر (حالة، مبلغ، تاريخ...) — واستبدالها بـ`assertOwnedByOrg` كان
سيضيف استعلاماً مكرراً بلا أي فائدة أمنية حقيقية (الفحص القائم يعمل
ومُختبَر أصلاً). المعيار المعتمَد: استبدال فقط حين يكون السجل **مجلوباً
حصراً للتحقق ولا يُستخدَم بعدها إطلاقاً**.

**استُبدلت فعلياً** (9 مواقع، 5 موديولات):
- `fleet`: `assignDriver` (driverId)، `createTrip` (driverId + grantId)،
  `createFuelLog` (vehicleId)، `createMaintenanceRecord` (vehicleId)
- `procurement`: `createPurchaseRequest` (grantId)، `createPurchaseOrder` (vendorId)
- `beneficiaries`: `addCaseNote` (caseId)
- `cfm`: `addComplaintUpdate` (complaintId)
- `hr`: `addPayrollAdjustment` (employeeId)
- `vendors`: `submitVendorRating` (vendorId + relatedPoId + relatedGrnId)،
  `flagEmergencyProcurement` (prId)

**تُركت عمداً كما هي** (فحص يدوي صحيح ومُختبَر، لا فائدة من تغييره):
- `hr`: `approvePayroll`/`markPayrollPaid` (`run.status` يُستخدَم بعد الفحص)،
  `updateContractAllowances` (`contract.*Allowance` تُستخدَم لـ`oldValues`)،
  `deletePayrollAdjustment` (`adj.consumedInRunId` و`oldValues`)
- `accounting`: كل المواقع الثلاثة (`period.isClosed`،
  `entry.*` لعكس القيد، `period` بمعاملة عكس القيد)
- `grants`: `approveGrant` (`existing.status` لـ`canTransition`)،
  `checkBudgetCeiling` (`line.planned/committed/spentAmount` هي بيت القصيد)
- `inventory`: `postDepreciation` (`asset.usefulLifeYears/purchaseCost`)
- `vendors`: `scoreBid` (`criteria.tenderId/maxScore`)
- `beneficiaries`: `verifyBeneficiary` (`existing.verificationStatus` لسجل التدقيق)
- `settings`: لا توجد حالات IDOR من هذا النمط بالملف أصلاً

**ملاحظة منهجية**: أول محاولة على `verifyBeneficiary` استُبدلت خطأً ثم
رُصد الخطأ فوراً عبر فشل اختبار حقيقي (`existing.verificationStatus`
كان يُستخدَم لاحقاً بسجل التدقيق) — تراجعنا وأعدنا الفحص اليدوي. هذا
يوضّح لماذا لم يُعتمَد الاستبدال الشامل الأعمى: بعض حالات إعادة
الاستخدام غير واضحة من أول قراءة سريعة للسطر.

### تحقق فعلي
`npx tsc --noEmit` → نظيف بالكامل.
`npx vitest run` → **332/332 ناجح** (نفس العدد الإجمالي — اختبارات IDOR
المعاد كتابتها لتعكس محاكاة `assertOwnedByOrg` بدل `findFirst` مخصص، لا
اختبارات جديدة صافية).

### الملفات المعدَّلة بهذه الجولة
- `src/modules/{fleet,procurement,beneficiaries,cfm,hr,vendors}/actions.ts`
- ملفات الاختبار المقابلة بكل موديول من الستة أعلاه — تحديث المحاكاة
  لتعكس `assertOwnedByOrg` بدل `db.query.<table>.findFirst` المباشر

### ما تبقى مفتوحاً بعد v38 (بلا تغيير عن v37 غير البند 3 أعلاه)
1. integration test حقيقي ضد Postgres فعلي متعدد الاتصالات (mocked فقط حالياً)
2. audit immutability الكاملة عبر KMS/REVOKE/WORM
3. ~~تسوية دورية لـ`items.currentStock` مقابل `stock_ledger`~~ — أُنجزت v39 §3
4. ~~توحيد نمط idempotency بـ`processPayroll` مع بقية العمليات المالية~~ — أُنجزت v39 §1
5. ~~قفل atomic لمتوسط `submitVendorRating`/`submitBidScore` التراكمي~~ — أُنجزت v39 §2
6. migration 016 لم تُختبَر بعد ضد بيانات إنتاج حقيقية (الأداة جاهزة، التنفيذ ينتظر بيئة حقيقية)
7. لا اختبار فعلي لتسليم بريد `SECURITY_ALERT_EMAIL`، ولا KMS/HSM خارجي (كما بv37)

## v39 — إكمال بنود المجموعة (أ) المفتوحة: idempotency، تحديث ذرّي، تسوية مخزون دورية

### 1. توحيد idempotency في `processPayroll`
استُبدل الفحص المباشر لعدم تكرار كشف الرواتب بـ`withIdempotency` (نفس
نمط `createDistribution`). **قرار تصميم لازم**: فحص "هل يوجد كشف مكرر
لنفس الفترة؟" انتقل *داخل* الـcallback الممرَّر لـ`withIdempotency`، لا
قبله — لو تُرك خارجها، إعادة إرسال نفس الطلب (نفس `idempotencyKey`) بعد
نجاحه كانت ستصطدم بهذا الفحص وتُرجع خطأ "كشف مكرر" بدل استرجاع نتيجة
النجاح الأصلية المخزَّنة، وهذا بالضبط عكس الغرض من التوحيد. قيد قاعدة
البيانات الحقيقي (`payroll_runs_org_period_uidx`, migration 015) يبقى
كما هو دون تغيير — يحمي من تزامن حقيقي بمفتاحين *مختلفين* أو بلا مفتاح
إطلاقاً، وهي حماية لا تغطيها `withIdempotency` (التي تحمي فقط تكرار نفس
المفتاح). المعامل الجديد (`idempotencyKey?`) اختياري وبآخر الترتيب —
الاستدعاءات الحالية (`NewPayrollButton`/`PayrollRunButton`) لا ترسله بعد
ولم تتأثر. 4 اختبارات جديدة بـ`hr/__tests__/actions.test.ts`.

### 2. تحديث ذرّي حقيقي لـ`submitVendorRating` و`submitBidScore`
استُبدل نمط "اقرأ كل السجلات → احسب المتوسط بالذاكرة → اكتب" بتحديث ذرّي
عبر subquery SQL aggregate (`AVG`/`SUM÷COUNT DISTINCT`) داخل نفس معاملة
الإدراج/التحديث. الآلية: `UPDATE` نفسه يحجز قفل الصف المستهدَف
(`vendors`/`tenderBids`) على مستوى postgres — معاملتان متزامنتان لنفس
المورد/العرض تتسلسلان تلقائياً (الثانية تُحجَب حتى commit الأولى، ثم
تُعيد حساب الـsubquery من جديد فترى تقييم الأولى المُثبَّت) — لا فقدان
تحديث (lost update) ممكن، بخلاف النمط القديم.

`submitBidScore` تحديداً لم يكن له **أي تغطية اختبارية سابقة على
الإطلاق** — تحقّقنا من هذا صراحة قبل التعديل. أُضيف ملف اختبار مستقل
`vendors/__tests__/bid-score-actions.test.ts` (7 اختبارات) بدل الدمج
بملف `actions.test.ts` الحالي (بُني حول جداول مختلفة تماماً).

**نطاق مقصود لم يُلمَس عمداً**: فحص "هل يوجد تقييم سابق لنفس
المقيّم/المعيار/العرض؟" بـ`submitBidScore` يبقى check-then-act تطبيقي
بلا قفل DB حقيقي — تبنّي upsert ذرّي له يحتاج أولاً UNIQUE index جديد
غير موجود بالمخطط الحالي (migration 017 جديدة)، خارج نطاق الطلب المحدَّد
صراحة بـ"حساب المتوسط التراكمي". موثَّق كتعليق مباشر بموضعه بالكود.

### 3. `reconcileStockLedger` — تسوية دورية للمخزون
دالة جديدة (`src/core/inventory/reconcile-stock.ts`) + سكريبت تشغيل
(`scripts/reconcile-stock-ledger.ts`) بنفس بنية
`verify-audit-integrity.ts`/`scheduled-check.ts` تماماً. تقارن
`items.currentStock` (الكاش) بمجموع حركات `stock_movements` الفعلية
(دفتر الحركات — ملاحظة: لا جدول منفصل باسم `stock_ledger` فعلياً؛ دُمج
بـ`stock_movements` منذ v32، راجع تعليق المخطط).

**تحقّقنا صراحة قبل الكتابة** أن `receiveStock`/`issueStock`
(`stock-movement.ts`) هما المسار *الوحيد* الذي يكتب `currentStock` بكل
المشروع — لا مسار آخر. أي انحراف تكتشفه هذه المهمة إذن مصدره خارج
التطبيق نفسه (تدخل يدوي بقاعدة البيانات، استعادة نسخة احتياطية جزئية،
خلل مستقبلي يخالف هذا الافتراض).

**تسجيل وتنبيه فقط — لا تصحيح تلقائي**، كما طُلب صراحة: لا `db.update`
إطلاقاً بمسار هذه الدالة (اختبار مخصص يتحقق من هذا). التنبيه داخلي فقط
(بلا قناة بريد أمني خارجية منفصلة كبنَد كسر سلسلة التدقيق) — هذا انحراف
محاسبي يحتاج مراجعة بشرية لتحديد الاتجاه الصحيح للتصحيح، لا مؤشر تلاعب
أمني بالضرورة. يُرسَل لأدوار `finance_manager`/`warehouse_manager`
(بالإضافة لـ`admin`/`super_admin`) — لا يوجد دور "محاسب" منفصل بمخطط
الأدوار الحالي (`userRoleEnum`). 7 اختبارات جديدة.

### تحقق فعلي نهائي (v39)
`npx tsc --noEmit` → نظيف بالكامل.
`npx vitest run` → **350/350 ناجح** (332 من v38 + 4 اختبارات idempotency
+ 7 اختبارات submitBidScore الجديدة كلياً + 7 اختبارات reconcileStockLedger).

### الملفات المضافة/المعدَّلة بهذه الجولة
- `src/modules/hr/actions.ts` (+اختبارات) — idempotency لـ`processPayroll`
- `src/modules/vendors/actions.ts` — تحديث ذرّي لـ`submitVendorRating`/`submitBidScore`
- `src/modules/vendors/__tests__/actions.test.ts` — إعادة كتابة اختبارات submitVendorRating للبنية الجديدة
- `src/modules/vendors/__tests__/bid-score-actions.test.ts` — **جديد بالكامل**
- `src/core/inventory/reconcile-stock.ts` — **جديد**
- `src/core/inventory/__tests__/reconcile-stock.test.ts` — **جديد**
- `scripts/reconcile-stock-ledger.ts` — **جديد**
- `DEPLOYMENT_GUIDE.md` — إضافة مهمة التسوية الدورية لقائمة مهام cron (راجع §الجدولة الدورية)

### ما تبقى مفتوحاً بعد v39
1. integration test حقيقي ضد Postgres فعلي متعدد الاتصالات (mocked فقط حالياً)
2. audit immutability الكاملة عبر KMS/REVOKE/WORM
3. upsert ذرّي لفحص "تقييم مكرر" بـ`submitBidScore` (يحتاج migration 017 — UNIQUE index جديد)
4. migration 016 لم تُختبَر بعد ضد بيانات إنتاج حقيقية (الأداة جاهزة، التنفيذ ينتظر بيئة حقيقية)
5. لا اختبار فعلي لتسليم بريد `SECURITY_ALERT_EMAIL`، ولا KMS/HSM خارجي (كما بv37)
6. جدولة `reconcile-stock-ledger.ts` الفعلية على Render/GitHub Actions لم تُفعَّل بعد — السكريبت جاهز، التفعيل قرار تشغيلي بيئة حقيقية (نفس حالة `verify-audit-integrity.ts`)


