# القائمة النهائية قبل النشر — v33

## ✅ ما تم التحقق منه فعلياً في جلسة v33 (مراجعة ذاتية — طلب المستخدم صراحة فحص التناقضات)
- [x] `npx tsc --noEmit` — 0 أخطاء | `npx vitest run` — **269/269 ناجح** (كانت 261 بنهاية v32)
- [x] **إصلاح حرج**: `withIdempotency` كانت تترك حجزاً عالقاً للأبد عند أي `throw` من داخل `fn()` — وهذا المسار الأشيع فعلياً (`createDistribution`, `createGoodsReceiptNote` يرميان استثناءً عند فشل تحقق عمل). أي إعادة محاولة بنفس المفتاح بعد فشل كانت تصطدم بـ"قيد التنفيذ حالياً" للأبد، حتى بعد تصحيح البيانات. أُصلح: أي فشل (رمي أو `{success:false}`) يفكّ الحجز فوراً.
- [x] أُضيف تحقق تطبيقي مطابق لكل CHECK constraint جديد أضفناه بv32 (مدين+دائن معاً، مبلغ سالب، سطر فارغ، entryType غير صحيح) — قبل هذا كان أي خرق سيصل المستخدم كرسالة Postgres خام
- [x] فحصنا يدوياً: لا مسار كود يتجاوز الدوال الذرّية الجديدة للمخزون، لا تعارض بين الـtriggers الجديدة وأي كود موجود مسبقاً، كل قيم entryType الفعلية متوافقة مع القيد الجديد، ترتيب الاعتماديات بmigration 001 سليم، migration 013 آمن لإعادة التشغيل رغم عدم idempotency حرفياً (بفضل `run-migrations.ts`)

## ✅ ما تم التحقق منه فعلياً في جلسة v32 (مراجعة خارجية ثانية أعمق + تصحيح ذاتي)
- [x] `npx tsc --noEmit` — 0 أخطاء | `npx vitest run` — **261/261 ناجح** (كانت 250 بنهاية v31)
- [x] **تصحيح ذاتي**: دمج جدول `stock_ledger` (أنشأناه بالخطأ بv31) داخل `stock_movements` الأصلي غير المُستخدَم سابقاً — بدل جدولين متوازيين
- [x] CHECK constraints حقيقية على مستوى DB: توازن سطر القيد، منع مدين+دائن معاً، كمية موجبة، enum صحيح لـ movement_type/entry_type (كانت صفر تماماً)
- [x] Trigger يمنع تعديل/حذف سطر أو قيد بعد ترحيله — DB-level، مو منطق تطبيق فقط
- [x] Trigger يفرض توازن مدين=دائن عند الترحيل كخط دفاع أخير
- [x] Trigger يمنع أي تعديل/حذف على `audit_logs` نهائياً (append-only حقيقي بدون REVOKE)
- [x] `createAuditLog` صار ذرّياً فعلياً (`tx` اختيارية) مع `createJournalEntry`, `postJournalEntry`, `reverseJournalEntry`, `createDistribution`
- [x] فحصنا فعلياً وليس نظرياً كل الـ22 استعلام `findMany` بـ`actions.ts`/`lib` — أغلبها مقيّد بمفتاح عمل طبيعي (غير خطير فعلياً)، لكن **اكتشفنا 59 استدعاء بلا حد بملفات `page.tsx`** (الخطر الحقيقي) وأصلحنا 4 منها (`vendors`, `grants`, `procurement`, `fleet`) بأداة pagination موحّدة
- [x] بنينا `createGoodsReceiptNote` كاملة من الصفر (كانت غائبة 100%) بعد إضافة الربط الناقص أصلاً بين `purchase_order_items` والمخزون (`item_id`) — migration 014

## ✅ ما تم التحقق منه فعلياً في جلسة v31 (إصلاحات صحة تشغيلية بناءً على مراجعة خارجية)
- [x] `npx tsc --noEmit` — 0 أخطاء (بعد كل الإصلاحات أدناه)
- [x] `npx vitest run` — **250/250 اختبار ناجح** (كانت 81 قبل هذه الجلسة؛ 169 اختباراً جديداً/مُحدَّثاً)
- [x] إصلاح Race Condition بخصم المخزون — خصم ذرّي بعبارة SQL واحدة (`src/lib/inventory/stock-movement.ts`) + قيد `CHECK (current_stock >= 0)` بقاعدة البيانات
- [x] إصلاح ثغرة ترحيل قيد محاسبي بعد إغلاق فترته (`postJournalEntry` كان يتحقق عند الإنشاء فقط، ليس عند الترحيل)
- [x] إضافة طبقة idempotency عامة (`src/lib/idempotency/guard.ts`) مربوطة بـ `createJournalEntry` و`createDistribution`
- [x] إضافة محرك `reverseJournalEntry` (كان غائباً بالكامل — لا وسيلة لتصحيح قيد مرحَّل سوى التعديل المباشر)
- [x] إضافة `stock_ledger` (AVCO) كمصدر حقيقة للتكلفة، بدل `items.currentStock` وحيداً
- [x] إضافة hash chain لسجل التدقيق (`prevHash`/`hash` + `verifyAuditChain`) — **tamper-evident وليس immutable مطلق**، الحد موثّق بالتفصيل في `SECURITY_NOTES.md`
- [x] **ثغرة IDOR إضافية اكتُشفت وأُصلحت**: `createDistribution` لم يكن يتحقق أن `beneficiaryId`/`grantId` المرسَلين فعلاً تابعان لمنظمة الطلب (فقط `organizationId` نفسه كان محمياً) — تم إصلاحها + اختباران مخصَّصان لهذا السيناريو تحديداً
- [x] migration جديدة `013_operational_integrity.sql` مضافة لقائمة `run-migrations.ts` (لم تُشغَّل بعد ضد DB حقيقي بهذه الجلسة — راجع البند أدناه)

## ✅ ما تم التحقق منه فعلياً في هذه الجلسة
- [x] `npx tsc --noEmit` — 0 أخطاء
- [x] `npm run build` — نجح، 41 صفحة
- [x] `npx vitest run` — 81/81 اختبار ناجح
- [x] `npm run db:migrate` ضد PostgreSQL حقيقي محلي — 9/9 migrations نجحت
- [x] `npm run db:seed` — نجح
- [x] تسجيل دخول حقيقي عبر NextAuth (credentials flow كامل مع CSRF)
- [x] فحص HTTP لكل صفحة جديدة ببيانات حقيقية مُدرجة يدوياً — كلها 200، صفر أخطاء في سجل الخادم
- [x] **مراجعة أمنية شاملة لكل الكود (وليس فقط الوحدات الجديدة) لثغرة IDOR** — تدقيق كل استدعاءات `db.update`/`db.delete` في كل ملفات `actions.ts` (12 ملف) وكل مسارات `api/`، وإصلاح كل حالة كانت تصفّي فقط بـ`id` بدون التحقق من `organizationId`. شمل هذا وحدات قديمة (grants, procurement, vendors, hr) لم تُبنَ في هذه الجلسة. أُعيد التحقق الكامل (build + tests + تشغيل حقيقي) بعد كل إصلاح.

## ⚠️ ما لم يُختبَر بعد (يلزم اختباره يدوياً قبل الاعتماد النهائي)
- [ ] **جديد v32**: تطبيق نفس نمط pagination على 55 استدعاء `findMany` متبقٍّ بملفات `page.tsx` (صفحة صفحة — القائمة الكاملة تحتاج مراجعة، بعضها "عرض شامل" مقصود زي kanban)
- [ ] **جديد v32**: بناء واجهة (UI) فعلية لـ`createGoodsReceiptNote` — الـaction جاهزة ومُختبَرة، ما فيها نموذج/صفحة تستدعيها بعد
- [ ] **جديد v32**: ربط `receiveStock`/`issueStock` بإنشاء قيد محاسبي تلقائي (GL) — غير موجود حالياً إلا بمنطق الإهلاك، لو محتاجينه لكل حركة مخزون فهذا تصميم إضافي
- [ ] **جديد v32**: تشغيل migration 013+014 فعلياً ضد PostgreSQL حقيقي (لم تُشغَّلا بعد بهذه البيئة)
- [ ] **جديد v31**: تشغيل migration `013_operational_integrity.sql` فعلياً ضد PostgreSQL حقيقي (تم كتابتها ومراجعتها، لم تُشغَّل بهذه الجلسة لعدم توفر اتصال DB بالبيئة الحالية)
- [ ] **جديد v31**: اختبار تزامن حقيقي لـ `issueStock`/`receiveStock` بعدة اتصالات فعلية ضد Postgres (الاختبار الحالي يحاكي الخوارزمية فقط، وليس تزامناً فعلياً على القرص)
- [ ] **جديد v31**: مراجعة نمط "فحص ملكية السجلات المرتبطة" (لا فقط `organizationId` المُرسَل) بشكل منهجي عبر بقية الموديولات (procurement, hr, fleet) — أُصلح فقط في `createDistribution` بهذه الجلسة، بقية الموديولات لم تُراجَع لنفس النمط تحديداً
- [ ] **جديد v31**: ربط طبقة idempotency ببقية العمليات المالية/المخزنية الحساسة (receipts, payroll runs, purchase orders) — حالياً مربوطة فقط بـ createJournalEntry وcreateDistribution
- [ ] إرسال نموذج فعلي (submit) عبر المتصفح لكل نموذج جديد (تم اختبار العرض/القراءة فقط عبر curl، الكتابة عبر server actions مُغطاة بالاختبارات الوحدوية فقط)
- [ ] سيناريو انقطاع الاتصال الفعلي (Offline Mobile) — إيقاف الشبكة من DevTools، تسجيل مستفيد، إعادة الاتصال، التأكد من المزامنة
- [ ] تسجيل مستخدمين بأدوار مختلفة (finance, procurement, hr, viewer) والتأكد من قيود الصلاحيات الجديدة (beneficiaries, partners, cfm) تعمل كما هو متوقع
- [ ] اختبار تدفق الفحص المسبق الكامل: تسجيل شريك → فحص "تطابق مؤكد" → التأكد من حظره تلقائياً من `dueDiligenceStatus`
- [ ] محاولة استغلال IDOR فعلية (penetration test) بحساب مستخدمَين من منظمتين مختلفتين، للتأكد الميداني من فعالية الإصلاحات
- [ ] مراجعة قانونية/امتثال: هل التوثيق اليدوي لـAnti-Terrorism Screening كافٍ لمتطلبات مانحيكم تحديداً، أم يلزم اشتراك آلي حقيقي؟

## 🔒 قبل الإنتاج الفعلي (بيانات حقيقية)
- [ ] تغيير `AUTH_SECRET` لقيمة عشوائية فعلية (`openssl rand -base64 32`) — **لا تستخدموا القيمة التجريبية**
- [ ] تغيير كلمة مرور admin الافتراضية فوراً بعد أول دخول
- [ ] تفعيل HTTPS إجباري على النطاق
- [ ] مراجعة كل الصلاحيات (`ROLE_DEFAULTS` في `src/lib/permissions/service.ts`) حسب هيكلكم التنظيمي الفعلي
- [ ] نسخ احتياطي تلقائي لقاعدة البيانات مُفعَّل قبل أي بيانات حقيقية

## 📌 القيود المعروفة (راجع README.md قسم "قيود معروفة" للتفاصيل الكاملة)
Anti-Terrorism Screening توثيق يدوي فقط · GIS Mapping يعرض السجلات ذات الإحداثيات المُدخَلة يدوياً فقط · Offline Mobile مفعَّل لنموذج تسجيل المستفيدين فقط حالياً · seed.ts لا ينشئ منحاً تجريبية.

